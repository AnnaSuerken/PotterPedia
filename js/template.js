function getHighlightBubble(index){
    return `<div class="content-bubble">
          <img src="${characterArray[0].image}" alt="" class="character-img" />
          <h2>${characterArray[0].name}</h2>
          <p>${characterArray[0].alias}</p>
        </div>`
}